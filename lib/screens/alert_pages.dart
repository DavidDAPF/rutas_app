import 'package:flutter/material.dart';

class AlertPages extends StatelessWidget {
  const AlertPages({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('AlertPages'),
      ),
    );
  }
}
//hola es una prueba